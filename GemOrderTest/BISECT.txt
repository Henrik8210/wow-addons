GemOrderTest bisect log (logout debugging)
==========================================
Disable main GemOrder. Enable only GemOrderTest.

FAKE v0.6.0 - FAIL (still had v0.7.73 code: stockWatcher, order dialog, StaticPopup, etc.)

v0.6.0-r1 - TESTING NOW
  Removed (NOT in real v0.6.0):
  - stockWatcher BAG_UPDATE auto-share (added v0.7.28)
  - Order dialog + overlay (added ~v0.7.3x)
  - StaticPopup close confirm (added v0.7.55)
  - Delete order button (added v0.7.40)
  - UI init on login — UI now loads only on first /gotest or minimap click

  Still runs at login: guild sync, bag scan if in workshop, minimap button, CHAT_MSG_ADDON

If v0.6.0-r1 PASS without opening UI: bug is in stripped UI/load code.
If v0.6.0-r1 FAIL without opening UI: bug is in sync/minimap/login hooks, not UI panels.

Commands: /gotest
